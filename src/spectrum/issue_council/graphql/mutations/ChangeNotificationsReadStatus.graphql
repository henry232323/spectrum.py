mutation ChangeNotificationsReadStatus($input: ChangeNotificationsReadStatusInput!) {
    changeNotificationsReadStatus(input: $input) {
      code
      success
      message
      problem {
        ...ProblemDetails
      }
    }
  }
